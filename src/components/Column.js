

const Column = (props) => {
  // const name = [];
  // const columnTitle = name;

  return (
    <div>
      <h3>{props.columnTitle}</h3>
      {/* <button onClick={addColumn}>+</button> */}
    </div>
  );
};
export default Column;
